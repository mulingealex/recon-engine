# Resources

Supporting assets for the reconnaissance engine.

| Path | Purpose |
|------|---------|
| `templates/` | Starter templates for manifests / indexes |
| `schemas/` | Reserved for JSON Schema definitions |
| `contracts/` | Reserved for interface contracts |
| `fixtures/` | Reserved for test fixtures |

Runtime writers remain the source of truth for generated artifact formats.
