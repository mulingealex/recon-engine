# Scripts

Operator helper scripts may live here. Portable examples currently live under [`examples/`](../examples/).

Suggested future helpers:

- Environment preflight (`dig`/`curl`/`nmap`/`openssl` presence)
- Output directory cleanup that preserves `.gitkeep`
- Evidence pack bundling into `deliverables/`
